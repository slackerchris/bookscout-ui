services:
  bookscout-ui:
    image: bookscout-ui:latest
    build: .
    container_name: bookscout-ui
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - bookscout

networks:
  bookscout:
    external: true
